/** Browser-safe private document schema and immutable command behavior. */
export { documentContentSchema, documentSchema, documentCommandSchema, createDocument, changeDocument } from "./engine";
export type { WorkspaceDocument } from "./engine";
